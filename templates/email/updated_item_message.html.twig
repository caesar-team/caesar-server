{% extends 'email/layout.html.twig' %}

{% block subject %}You have updates on the {{ env('APP_NAME')|default('Caesar.Team') }} secrets{% endblock %}

{% block body %}
    <h2>Hi!</h2>
    <p>You have updates on {{ itemsCount }} secrets that are shared with you.</p>
    <p><a href="{{ env('WEB_CLIENT_URL') }}">Join {{ env('APP_NAME')|default('Caesar.Team') }}</a></p>
    <p>After you log into Caesar.Team. you can accept or reject updates.</p>
{% endblock %}