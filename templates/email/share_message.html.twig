{% extends 'email/layout.html.twig' %}

{% block subject %}Invite to application{% endblock %}

{% block body %}
    {% set message = 'mail.share.created_link.message' %}
    {% if method = 'update' %}
        {% set message = 'mail.share.updated_link.message' %}
    {% endif %}
    {{ message | trans({'%url%': url}) }}
{% endblock %}