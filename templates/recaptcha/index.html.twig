<!DOCTYPE html>

<title>Hello RecaptchaController!</title>
<script src="https://www.google.com/recaptcha/api.js?render=6LcAeKMUAAAAAPnYs_hgIW4AwVATIn_r0NPpZpLI"></script>
<script>
    grecaptcha.ready(function() {
        grecaptcha.execute('6LcAeKMUAAAAAPnYs_hgIW4AwVATIn_r0NPpZpLI', {action: 'login'}).then(function(token) {
            var xhr = new XMLHttpRequest();
            var form = new FormData();
            form.append('_token', token);
            xhr.open('POST', '/recaptcha', true);
            console.log(token);
            xhr.send(form);
        });
    });
</script>
{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/html/src/Controller/RecaptchaController.php'|file_link(0) }}">src/Controller/RecaptchaController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/html/templates/recaptcha/index.html.twig'|file_link(0) }}">templates/recaptcha/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
